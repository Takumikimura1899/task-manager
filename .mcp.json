{
  "mcpServers": {
    "task-manager": {
      "command": "bun",
      "args": ["mcp/main.ts"],
      "env": {
        "MCP_ACCESS_TOKEN": "${MCP_ACCESS_TOKEN}",
        "MCP_AGENT_NAME": "AI Agent"
      }
    }
  }
}
