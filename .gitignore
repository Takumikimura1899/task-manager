node_modules/
.env.local
.env
# Convex 生成コードは CI の typecheck/build のためコミットする（schema/関数から
# 決まる純粋な生成物・機密なし）。AI 向け補助ドキュメントのみ ignore する。
convex/_generated/ai/
dist/
# vitest カバレッジ出力（CI ログの text レポータが主だがローカル生成物も除外）
coverage/
*.log
.DS_Store

# Playwright MCP の一時生成物
.playwright-mcp/
